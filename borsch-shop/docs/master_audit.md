# Master Audit: Уровень готовности и Тестовое покрытие (Backoffice)
Дата: 29 Марта 2026

## 1. Заглушки (Пустые страницы, нет функционала)
Следующие страницы только созданы (`page.tsx` по 10 строк) и не имеют функционала. Их нужно перенести:
- POS / Касса (`/pos`)
- Инвентаризация (`/stocktakes`)
- Списания (`/waste`)
- Сотрудники (`/workers`)
- Смены / Часы (`/shifts`)
- Документы (`/documents`)

## 2. Страницы без тестирования UI и модальных окон
Функционал реализован частично или полностью, но **нет ни одного UI-теста** для компонентов и кнопок:
- **Recipes (`/recipes`)**: Вся логика редактирования рецептов находится прямо в `page.tsx` (316 строк). Модальных окон нет, тесты интерфейса отсутствуют. 
- **Marketing (`/marketing`)**: Отдельный сложный дашборд рассылок, тесты отсутствуют.
- **Clients (`/clients`)**: Логика фильтрации и показа клиентов без тестов.
- **Landing Settings (`/landing-settings`)**: Настройки лендинга не покрыты тестами.
- **Translations (`/translations`)**: UI переводчика не тестировался.
- **Ai Settings (`/ai-settings`)**: Логика подключения LLM без тестов.
- **Dashboard (`/dashboard`)**: Виджеты мониторинга не покрыты.

## 3. Компоненты, которые уже имплементированы, но не имеют тестов
- `apps/backoffice/src/app/(protected)/suppliers/components/SupplierDetails.tsx`

## 4. Что УЖЕ ПОКРЫТО тестами (Прогоняются успешно - 35/35)
**Хранилища (Store / Business Logic):** 100% покрыты (15 файлов).
**Модальные окна (UI - React Testing Library):**
- `AssemblyEditModal.test.tsx`
- `SupplierEditModal.test.tsx`
- `MenuCategoriesModal.test.tsx`
- `MenuItemModal.test.tsx`
- `OrderEditModal.test.tsx`
- `InventoryItemModal.test.tsx`
- `QuantityModal.test.tsx`

## План работ (Новые задачи):
1. **[Реализуется сейчас]**: Написать тесты для `SupplierDetails.tsx`.
2. **[К выполнению]**: Вынести панель редактирования рецептов `/recipes` в отдельный `RecipeEditModal.tsx` и покрыть функциональными тестами.
3. **[К выполнению]**: Разработка с нуля функционала для пустых страниц (Касса POS, Списания, Инвентаризация и т.д.) по спроектированной архитектуре.

*Авто-анализ произведен агентом Antigravity.*
