# Недельный Спринт 1 (Завершённые задачи)

## Выполнено к 2026-04-04

### Базовые улучшения и Рефакторинг
- [x] **CI/CD & Naming Fixes**: Починены алиасы в `vitest` (из-за переименования ломались пути к `@rms/db-local`), исправлена ошибка синтаксиса в LandingClient. Вычищены последние упоминания "Borsch Shop" по всему коду и документации.
- [x] **Архитектурный рефакторинг**: Перевели `rms-ai-os` в парадигму Pocketbase Typegen.
- [x] **Global Alerts Transition:** Полный отказ от legacy `alert()` в пользу глобальной системы Toast-уведомлений (`useToastStore`), управляемых через middleware.

### Тестирование
- [x] **Тестирование (Репозитории)**: Добавлено абсолютное 100% покрытие Unit-тестами для ВСЕХ 19 репозиториев приложения.
- [x] **Тестирование (Сторы)**: Написаны тесты для 13+ Zustand сторов (`useMenuStore`, `useOrdersStore`, `useInventoryStore`, и др).
- [x] **Тестирование (UI Components)**: Написаны тесты React Testing Library для самых важных страниц. Запуск тестов стабилен (`vitest run src/__tests__`).

### Новые Фичи
- [x] **Weekly Subscriptions Logic:** Добавлен режим "Ожидание" (`pending`) для подписок.
- [x] **Navigation & Layout Refactoring:** Реорганизация левого сайдбара и топ-бара. Левое меню расширено всеми страницами системы (20+ разделов).
- [x] **CRM & Segmentation:** Добавлен инструмент сегментации ("Уснувшие клиенты", "Топ-клиенты" по LTV/Частоте) в модуль Клиенты.
- [x] **KDS Refactoring & Preorder Fixes:** Решена бага с логикой показа предзаказа (исправлено подтягивание поля `is_preorder_mode` из PB). Убран старый переключатель дат с лендинга. Кнопки Предзаказ теперь принудительно ставят оффсет завтрашнего дня, если включено в админке. Баги картинок на проде устранены.
- [x] **Dine-In & Table Sessions (Заказы в зал):** 
   - Создан отдельный интерфейс для управления столами `/tables`.
   - Активные заказы, привязанные к столу `table=X`, попадают в единый пречек.
   - [x] **Smart Seating:** Реализован алгоритм взвешенной рассадки (Weighted Optimization) с кнопкой быстрого выбора лучшего стола.
- [x] **Dynamic Pricing (Пиковые цены):**
   - Внедрена система наценок в зависимости от времени суток и остатков на складе.
   - Добавлен визуальный индикатор "🔥 ПИК" в Кассе.
- [x] **Staff Playbook (Trello-style):**
   - Создана страница `/workers/playbook` (Методичка) с интерактивным дашбордом в стиле Kanban через `@hello-pangea/dnd`.

### Интеграция AI
- [x] **Predictive Ordering (ИИ-закупки):** Реализован локальный ИИ-прогноз на основе техкарт и истории продаж за 7 дней.
- [x] **AI Advisor Integration:**
   - Внедрён компонент `AiInsightCard` на страницах Дашборд, Клиенты, Склад, Меню, Столы.
   - Добавлен хук `useAiAdvisor` и настроено переключение для локальной LLM в Настройках.
