rules:
  - "В каждом генерируемом роутере (API-Morph) добавляй JWT-проверку. ВАЖНО: всегда импортируй JWT так: `from fastapi.security import HTTPBearer, HTTPAuthorizationCredentials`."
  - "В UI-Morph при логине/оплате всегда симулируй защищенное соединение (хотя бы в UX показать замочек)."
