# RMS AI OS — Project Memory (Borsch Core MVP)

> **Статус**: Минимальный рабочий прототип (MVP) выделен в отдельный проект.
> **Расположение**: `/Users/klai/AI/mvp/borsch-core` 

## ⚙️ Локальный контекст (Стек и Архитектура)
- **Архитектура:** Monorepo (Turborepo + pnpm)
- **Стек Фронта:** Next.js 14+ (App Router), TailwindCSS, Zustand
- **Стек Бэка:** PocketBase (Hosted on VPS: `https://borsch.shop`)
- **Tauri / Оффлайн:** **Отключен**. Мы полностью отказались от старого механизма локальной SQLite+SyncEngine. Прямые `pb.collection` запросы к VPS.
- **Telegram:** Полностью работает на серверной стороне (VPS, файл `pb_hooks`). Фронтенд туда не лезет. 

## 📦 Структура Проекта
1. `apps/backoffice/` (порт 3001) — Админ-панель:
   - **Активные Модули:** 
     - `/pos` (Касса), `/orders` (Лента Заказов с интегрированной вкладкой `ClientsTab`), `/menu` (Меню).
     - **Склад и Закупки (Восстановлены и стандартизированы):** `/inventory` (Склад с категориями без Drag&Drop, с жесткой привязкой к кнопкам), `/recipes` (Умные рецепты с автоуказанием единиц измерения), `/purchases` (Закупки авто-считающие недостаток склада по активным заказам).
   - **Вырезано/Удалено:** Старая громоздкая CRM, Финансы, Списание, AI Sidebar, AI ассистент (заменены на минималистичные интеграции). 
2. `apps/landing/` (порт 3000) — Лендинг для клиентов (доставки/самовывоз).
3. `packages/core/` — Переиспользуемая бизнес-логика (Stores). Включает `useMenuStore`, `useOrdersStore`, `useAuthStore`, `useInventoryStore`, `useRecipesStore`.
4. `apps/backoffice/src-tauri` — Десктопная обертка на базе Tauri 2.0 (сохранение геометрии окна через `window-state:default` plugin).

## 🚀 Как запускать
```bash
# Из корня /Users/klai/AI/mvp/borsch-core
pnpm dev
# Поднимет landing на localhost:3000
# Поднимет backoffice на localhost:3001
```

## 🌍 Важные нюансы
- В `apps/backoffice/.env` лежит переменная соединения с БД: `NEXT_PUBLIC_PB_URL=https://borsch.shop`
- **Проблема с датами:** Функция `safeFormat` для дат `reservationDate / createdAt` решает проблему строковых дат старой БД.
- **Tauri Оффлайн отключен:** Но само Tauri приложение (macOS) собирается для обертки (`/deploy-desktop`).
- **Генерация Закупок:** Черновики заказов генерируются с учетом дефицита `(Заказы * Порции) - Склад`. Учитываются единицы измерения (`кг`, `упаковка`).

## ✅ Что сделали (Апрель 2026)
1. **Капитальный ремонт Модулей Backoffice**: Вернули и упростили Склад, Рецептуры, Закупки.
2. Отказались от Drag&Drop складов в пользу строгой Kanban-таблицы (добавление прям в колонку).
3. **Clients Tab:** Переписали логику клиентов — теперь это отдельный компонент `<ClientsTab />`, который агрегирует `LTV` звонков и заказов исторически, и имеет интерфейс 1-в-1 как `OrderCard`.
4. Ввели единицы измерения (`кг, г, л, шт, упаковка`) на складе. В рецептах эти единицы красиво подставляются сбоку.
5. Настроили сборку Tauri для Mac с сохранением размера/позиции окна.

## ⚡ Задача для следующего чата
1. **Сквозное тестирование (E2E / Playwright)**: Сделать реальный заказ через Лендинг (порт 3000) и проверить:
   - Моментальное появление в Кассе (Backoffice).
   - Отработку алгоритма генерации дефицита.
   - Работу Telegram Webhooks на VPS.

---
## 🛡️ GOLDEN SAFETY PROTOCOL (Защита от потери данных)
- **1. Переименование**: Если файл или папка «больше не нужны», я не удаляю их, а переименовываю в `[name]_BAK_[timestamp]`. Удалил — только если явная команда "удалить".
- **2. SAFE-COMMIT**: Перед любым рефакторингом (переписыванием модулей) — обязателен коммит.
- **3. Backup DB**: Конфиги и схемы хранить бережно, поскольку бэк у нас удаленный.
